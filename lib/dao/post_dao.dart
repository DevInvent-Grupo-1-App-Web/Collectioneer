class PostDao {
  insert() async {
    // Database db = await AppDatabase().openDB();
    // db.insert(AppDatabase().tableName, {
    //   'user_token': 'token
  }

  update() async {
    // Database db = await AppDatabase().openDB();
    // db.update(AppDatabase().tableName, {
    //   'user_token': 'token
  }

  delete() async {
    // Database db = await AppDatabase().openDB();
    // db.delete(AppDatabase().tableName, {
    //   'user_token': 'token
  }
}